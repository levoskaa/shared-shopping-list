import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.levos.shoppy',
  appName: 'Shoppy',
  webDir: 'dist/shared-shopping-list.client',
  bundledWebRuntime: false,
};

export default config;
